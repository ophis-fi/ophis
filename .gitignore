node_modules/
dist/
.next/
.turbo/
.vercel/
.wrangler/
target/
.env
.env.local
.env.*.local
.DS_Store
*.log
coverage/
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Local agent / tooling configs (not part of the public repo)
.claude/
.slither-out/

# Safe-drift-check rendered script (template lives at .tmpl)
infra/shared/cron/safe-drift-check.sh

# Private ops runbooks — contain host IPs, version pins, SSH endpoints, iptables rules
# These details enable recon if leaked. Keep on operator's machine only.
docs/operations/*.private.md
