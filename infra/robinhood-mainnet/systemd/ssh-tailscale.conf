# Install at /etc/systemd/system/ssh.service.d/tailscale.conf on Cadia.
[Unit]
Wants=tailscaled.service
After=tailscaled.service

[Service]
# sshd exits 255 when the Tailscale listen address is not ready at boot.
Restart=on-failure
RestartPreventExitStatus=
RestartSec=5s
