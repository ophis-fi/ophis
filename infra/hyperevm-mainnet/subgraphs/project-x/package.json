{
  "name": "project-x-subgraph",
  "description": "Project X (UniV3-fork) subgraph for HyperEVM mainnet — backs the Ophis baseline solver's UniV3 liquidity source. Phase 2 of the V3 wiring spec.",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "deploy:goldsky": "goldsky subgraph deploy project-x-hl/0.0.1 --path .",
    "deploy:selfhost:create": "graph create --node http://127.0.0.1:8020/ project-x-hl",
    "deploy:selfhost:deploy": "graph deploy --node http://127.0.0.1:8020/ --ipfs http://127.0.0.1:5001 project-x-hl",
    "auth:goldsky": "goldsky login"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.93.3",
    "@graphprotocol/graph-ts": "0.36.0"
  }
}
