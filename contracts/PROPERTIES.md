# Properties

> Generated by the Fizz skill (5 discovery agents + synthesizer). Each property is
> described in English and has one of three states:
> - `[ ]` pending — not yet implemented; `/fizz-convert` will pick it up
> - `[x]` implemented — Solidity exists in Properties.sol / handlers
> - `[-]` skipped / manual — `/fizz-convert` will NOT touch it. Use this for TODO stubs,
>   properties that need human judgment, or properties you add yourself later.
>
> Users may freely add their own entries (use any unused `GL-NN` / `SP-NN` ID) and mark
> them `[ ]` to have `/fizz-convert` implement them, or `[-]` to leave them as a spec
> note only.
>
> IDs are stable. Do not renumber.
>
> **Guarantee tag.** Every property is tagged `SHOULD-HOLD` or `EXPLORATORY`:
> - `SHOULD-HOLD` — explicitly guaranteed by docs/spec/standard or an exact identity
>   (evidence cited inline). If the campaign violates it, that is a **confirmed bug**.
> - `EXPLORATORY` — inferred, not explicitly promised. A violation is a **lead flagged
>   for human review**, not an automatic bug. Several EXPLORATORY entries below
>   (SP-21/SP-23/SP-24/SP-26) are **expected-violated** leads: the property is written to
>   fire so a human sees the call sequence — a fired assertion there is the intended signal
>   about the unenforced `manager() == address(allowListGuardian)` (X-1) binding, not a
>   harness bug.
>
> This tag is set at generation time so violations can be triaged without post-hoc
> severity guessing. If you edit a property's logic such that it is no longer guaranteed,
> change its tag to `EXPLORATORY`.
>
> Protocol: Ophis solver-allowlist governance layer — `AllowListGuardian` +
> `GPv2AllowListAuthentication`. A no-funds access-control state machine. Roles in the
> harness: `TIMELOCK_ROLE` (0x1111, also the EIP-1967 proxy admin), `GUARDIAN_ROLE`
> (0x2222), 3 generic actors (never a role). `manager` is deployed == the Guardian
> contract from tx 0 but is mutable at runtime.

## Global Properties
> Always-on invariants. Implemented as `public function property_*()` in `test/fizz/Properties.sol`.

- [-] **GL-01** — "Guardian authority" is the derived cross-contract condition `authentication.manager() == address(allowListGuardian)` (X-1); `AllowListGuardian.addSolver/removeSolver/setManager` succeed only while it holds, `setGuardian` is exempt (never forwards). X-1 must be recomputed live on every check, never cached in a ghost. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — AllowListGuardian NatSpec L20/L33 + G-1/G-2/G-6 + x-ray X-1; Priority: HIGH; Scope: always-on; Sources: ST-05, VS-03)
- [x] **GL-02** — No caller other than `timelock` can ever cause a solver-add, a manager handoff, or a guardian rotation through `AllowListGuardian` (its `_unauthorized` actor-callable paths never mutate state). (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — AllowListGuardian.sol:70 `onlyTimelock` (G-1), actors disjoint from TIMELOCK_ROLE; Priority: HIGH; Scope: always-on; Sources: ADV-01)
- [x] **GL-03** — No caller other than `guardian` can ever cause a solver removal through `AllowListGuardian`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — AllowListGuardian.sol:75 `onlyGuardian` (G-2); Priority: HIGH; Scope: always-on; Sources: ADV-02)
- [x] **GL-04** — On the authenticator called directly, the only addresses that can add/remove a solver are the current `manager`, and the only addresses that can setManager/proposeManager/cancelManagerTransfer are the current `manager` or the EIP-1967 admin — never the guardian, an arbitrary actor, or a stale `pendingManager`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:80 (G-6), :90-93 (G-7); Priority: HIGH; Scope: always-on; Sources: ADV-03, GOV-02)
- [x] **GL-05** — The EIP-1967 admin's authority over setManager/proposeManager/cancelManagerTransfer is never revoked by a manager handoff — it persists across arbitrarily many manager rotations. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:89-95, admin disjunct of G-7 is independent of `manager`; Priority: HIGH; Scope: always-on; Sources: GOV-01)
- [x] **GL-06** — The `admin == timelock` coincidence (both TIMELOCK_ROLE in the harness) is an unenforced deployment convention — nothing cross-checks `getAdmin() == allowListGuardian.timelock()`; a second instance of the X-1 unenforced-binding pattern (admin↔timelock). (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: MEDIUM; Scope: always-on; Sources: GOV-04)
- [-] **GL-07** — Once `manager()` moves off the Guardian, restoring the X-1 binding requires a DIRECT `authentication.setManager(Guardian)` / `proposeManager(Guardian)+accept` by the current manager/admin — never any call routed through `AllowListGuardian` (it has no accept/propose forwarder, and its 3 forwarders revert one level deeper). (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: MEDIUM; Scope: always-on; Sources: ST-23)
- [x] **GL-08** — Cross-contract storage isolation: no direct authenticator call ever mutates `AllowListGuardian`'s own storage (guardian/timelock/authenticator), and the only bridge from the Guardian to the authenticator is its 3 documented forwards (setGuardian never reaches the authenticator). (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — whole-file read: no back-references; Priority: MEDIUM; Scope: always-on; Sources: ST-21)
- [x] **GL-09** — No address becomes `manager` via `acceptManagership` unless it is the EXACT most-recently proposed address; a superseded/stale proposal (overwritten by a later `proposeManager`) can never be accepted. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:152 (G-8), :153 (G-9), x-ray I-4; Priority: HIGH; Scope: always-on; Sources: ST-13, ADV-04)
- [-] **GL-10** — `manager` is only ever written by exactly 3 functions: `initializeManager` (once), `setManager`, `acceptManagership`. Any change to `manager` after any other handler is a violation. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:72-75/112-125/151-158 are the only assign sites; Priority: HIGH; Scope: always-on; Sources: ST-16)
- [-] **GL-11** — `pendingManager` is only ever written by exactly 4 functions (proposeManager, acceptManagership, cancelManagerTransfer, setManager side-effect), and only `proposeManager` can leave it nonzero — the other three can only drive it to 0. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:112-125/139-142/151-158/164-168; Priority: HIGH; Scope: always-on; Sources: ST-17)
- [-] **GL-12** — `guardian` is only ever written by exactly 2 sites (constructor, setGuardian), both of which reject `address(0)`. No other function on either contract writes it. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — AllowListGuardian.sol:60/79-88/111-115 (G-3, G-5); Priority: HIGH; Scope: always-on; Sources: ST-18)
- [x] **GL-13** — `allowListGuardian.guardian() != address(0)` always holds. (`guardian`/`manager`/`pendingManager`/solver-set are otherwise freely bidirectionally mutable — none is monotonic; the only standing bound is guardian's non-zero-ness.) (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — constructor G-3 + setGuardian G-5, x-ray I-1; Priority: HIGH; Scope: always-on; Sources: VT-05, ST-18-corollary/I-1)
- [x] **GL-14** — `allowListGuardian.timelock()` and `allowListGuardian.authenticator()` are immutable and always equal their constructor values (TIMELOCK_ROLE / the authenticator address); zero post-construction writers (compiler-enforced). (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — AllowListGuardian.sol:52/55 `immutable`, x-ray I-2; Priority: LOW; Scope: always-on; Sources: ST-19, VT-02, VT-03)
- [-] **GL-15** — `manager` can reach `address(0)` ONLY via a direct `authentication.setManager(0)` (the I-3 caveat) — never via the Guardian's setManager (rejects 0, G-4), never via acceptManagership (requires pending != 0, G-8), never via propose+accept. (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:112-125 vs :151-158 (G-8), x-ray I-3; Priority: HIGH; Scope: always-on; Sources: ST-20)
- [x] **GL-16** — The EIP-1967 admin storage slot never drifts from its setup value for the whole campaign — no in-scope, fuzzer-reachable function calls `setAdmin`. (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — Helper.sol:9-13 is the only setAdmin site repo-wide; setter never called from either in-scope contract; Priority: HIGH; Scope: always-on; Sources: ST-22, VT-04, GOV-03)
- [x] **GL-17** — `initializeManager` can never succeed again after `setup()`'s single call — every fuzzer-reachable call reverts "Initializable: initialized", any caller, any argument, for the entire campaign (one-way `_initialized` latch, no de-init path). (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — Initializable.sol:38-55 single write site; Base.setup() consumes the one call; Priority: HIGH; Scope: always-on; Sources: VT-01, ADV-07, GOV-10)
- [x] **GL-18** — While `manager == address(0)` (reachable only via GL-15's direct path), no `onlyManager`-gated function (addSolver/removeSolver) is callable by any fuzzer-controlled address, and the Guardian's forwards also revert — only the immutable EIP-1967 admin can recover via setManager (fail-safe-by-design self-lockout state). (Category: VALID_STATE; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:79-82/112-125/89-95; Priority: HIGH; Scope: always-on (when in the manager==0 state); Sources: ST-20)
- [x] **GL-19** — While `pendingManager == X` (nonzero), `X` gains NO privilege other than calling `acceptManagership()` — being pending never lets X add/remove solvers or setManager/propose/cancel (those gate on current manager/admin; pendingManager is read in neither modifier). (Category: VALID_STATE; Guarantee: SHOULD-HOLD — pendingManager referenced nowhere in onlyManager :79-82 / onlyManagerOrOwner :89-95; Priority: HIGH; Scope: always-on (when a proposal is pending); Sources: GOV-06)

## Specific Properties
> Operation-gated postconditions. Implemented as `internal function property_*()` in `Properties.sol` and called from the relevant handler after `snapshotAfter()`.

- [x] **SP-01** — After a successful `addSolver(s)` the target is a solver; after a successful `removeSolver(s)` it is not — an unconditional absorbing end-state (remove always forces false, add always forces true, regardless of prior), and repeating either op is an idempotent no-revert. Holds on both the Guardian-forwarded and direct-authenticator paths. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — x-ray I-5; GPv2AllowListAuthentication.sol:177/188 + NatSpec "idempotent" L173/L184; Priority: HIGH; After: add/removeSolver handlers; Sources: RT-01, RT-02, RT-03, ST-14)
- [x] **SP-02** — `proposeManager(x)` then `cancelManagerTransfer()` (both via the admin branch) ends with `pendingManager == 0` and `manager` exactly equal to its value before the propose — neither function body writes `manager`. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:140 / :165-166, x-ray I-4; Priority: HIGH; After: cancelManagerTransfer handler; Sources: RT-04, ST-15)
- [x] **SP-03** — `cancelManagerTransfer()` with no pending proposal is a safe no-op: no revert, `pendingManager` stays 0, `manager` untouched; twice behaves as once. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — NatSpec L161-163 verbatim "No-op if no pending transfer exists"; Priority: MEDIUM; After: cancelManagerTransfer handler; Sources: RT-05, ST-11, GOV-07)
- [x] **SP-04** — A cooperative 4-step round trip (established manager M0 proposes B, B accepts, B proposes M0 back, M0 accepts) returns `manager` to M0 with `pendingManager == 0` — the two-step transfer is fully reversible with no residual/dust state. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: MEDIUM; After: new bespoke round-trip handler; Sources: RT-06)
- [x] **SP-05** — A successful `AllowListGuardian.addSolver(s)` (as timelock) results in `isSolver(s) == true`, and can only succeed while X-1 holds (else it reverts inside the forward). (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — G-1, G-6, x-ray X-1; Priority: HIGH; After: allowListGuardian_addSolver; Sources: ST-01)
- [x] **SP-06** — A successful `AllowListGuardian.removeSolver(s)` (as guardian) results in `isSolver(s) == false`, conditional on X-1. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — G-2, G-6, x-ray X-2; Priority: HIGH; After: allowListGuardian_removeSolver; Sources: ST-02)
- [x] **SP-07** — A successful `AllowListGuardian.setManager(m != 0)` (as timelock) leaves BOTH `authentication.manager() == m` AND `pendingManager() == 0` (the forward clears any dangling proposal); `m == 0` is rejected before the forward (G-4). (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — G-4; GPv2AllowListAuthentication.sol:112-125; Priority: HIGH; After: allowListGuardian_setManager; Sources: ST-03)
- [x] **SP-08** — A successful `AllowListGuardian.setGuardian(g != 0)` (as timelock) sets `guardian == g` and touches NOTHING on the authenticator (no forward call at all). (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — AllowListGuardian.sol:111-115 (G-5), no authenticator call in body; Priority: MEDIUM; After: _allowListGuardian_setGuardian; Sources: ST-04)
- [x] **SP-09** — A direct `authentication.addSolver(s)` succeeds iff the effective caller equals `manager()` at call time (checked against the CURRENT mutable manager, not a fixed role), and then `isSolver(s) == true`. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — G-6; Priority: HIGH; After: gPv2AllowListAuthentication_addSolver*; Sources: ST-06)
- [x] **SP-10** — A direct `authentication.removeSolver(s)` succeeds iff the effective caller equals `manager()` at call time, and then `isSolver(s) == false`. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — G-6, :187-190; Priority: HIGH; After: gPv2AllowListAuthentication_removeSolver*; Sources: ST-07)
- [x] **SP-11** — `addSolver(a)` / `removeSolver(a)` flip `isSolver` ONLY for `a` — no other probed address's membership changes, and `manager`/`pendingManager`/`guardian`/`timelock`/`authenticator` are untouched (frame condition). (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:176-190 single mapping write; Priority: HIGH; After: add/removeSolver handlers; Sources: ST-08)
- [x] **SP-12** — A successful direct `authentication.setManager(m)` is an atomic pair: `manager == m` (for ANY `m`, including 0) AND `pendingManager == 0`, regardless of the prior pending value. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:112-125, x-ray I-3; Priority: HIGH; After: gPv2AllowListAuthentication_setManager*; Sources: ST-09, VS-02)
- [x] **SP-13** — A successful `proposeManager(m)` sets `pendingManager == m` UNCONDITIONALLY (from 0 or any other nonzero value) with `manager` unchanged; it is the only function that raises `pendingManager` off zero, and never reverts for any input (incl 0/self/current manager). (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:139-142 (corrects x-ray I-4 "0→X" wording); Priority: HIGH; After: gPv2AllowListAuthentication_proposeManager*; Sources: ST-10, GOV-07)
- [x] **SP-14** — A successful `acceptManagership()` is an atomic pair: `manager` becomes exactly the value `pendingManager` held immediately before, AND `pendingManager` is cleared to 0 — and it is the only function where `manager` takes on the prior pending value. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:151-158 (G-8/G-9), x-ray I-4; Priority: HIGH; After: gPv2AllowListAuthentication_acceptManagership*; Sources: ST-12, VS-01)
- [x] **SP-15** — Strict converse of the accept edge: whenever `manager` changes to equal what was `pendingManager` before the call, the call must have been `acceptManagership` by the pending address (disambiguated via a `lastCallWasAccept` tag from a coincidental `setManager(sameValue)`). (Category: STATE_TRANSITION; Guarantee: EXPLORATORY; Priority: MEDIUM; After: acceptManagership + setManager handlers; Sources: VS-04)
- [x] **SP-16** — `acceptManagership` can never zero the manager: because it requires `pendingManager != 0` before assigning, `manager` after any successful accept is nonzero — the two-step rail is zero-brick-proof (unlike single-step setManager). (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:152 (G-8); Priority: MEDIUM; After: gPv2AllowListAuthentication_acceptManagership*; Sources: GOV-08, ADV-13)
- [x] **SP-17** — Cross-role rejection at the Guardian layer: `GUARDIAN_ROLE` can never succeed calling the Guardian's timelock-only functions (addSolver/setManager/setGuardian), and `TIMELOCK_ROLE` can never succeed calling the guardian-only `removeSolver` — a compromised guardian is bounded to remove-only. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — AllowListGuardian.sol:70/75 (G-1/G-2), x-ray verdict; Priority: HIGH; After: new cross-role Guardian handlers; Sources: ADV-05)
- [x] **SP-18** — The guardian can never entrench or rotate itself — `guardian` only ever changes via a call where `msg.sender == timelock`, never the guardian acting on its own (including a self-re-election attempt). (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — AllowListGuardian NatSpec L57-59 "Rotatable only via the timelock so a compromised guardian cannot entrench itself"; Priority: HIGH; After: setGuardian handlers + new asGuardian variant; Sources: ADV-06)
- [x] **SP-19** — No caller outside {current manager, EIP-1967 admin} can set, overwrite, or clear `pendingManager` — an attacker cannot squat, redirect, or grief an in-flight manager-transfer proposal. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:139-142/164-168 (G-7); Priority: HIGH; After: unauthorized proposeManager/cancelManagerTransfer handlers; Sources: ADV-14)
- [x] **SP-20** — The on-chain contracts enforce NO time/block delay: `AllowListGuardian`'s timelock-only functions succeed instantly for `msg.sender == timelock` with two back-to-back calls in the same block both succeeding — the ">= 24h" delay lives entirely in the off-chain OZ TimelockController at the timelock address. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — grep: zero block.timestamp/number/minDelay refs in either contract; modifiers are pure msg.sender equality; Priority: HIGH; After: allowListGuardian_addSolver back-to-back; Sources: GOV-05)
- [x] **SP-21** — [EXPECTED-VIOLATED LEAD] An attacker must never gain instant, undelayed, unmonitored solver-add capability: once `manager()` is set to `GUARDIAN_ROLE` (via the direct authenticator propose+accept or setManager, all individually authorized), GUARDIAN_ROLE can `addSolver` directly with no timelock delay and without emitting `SolverAddedViaTimelock`, inverting the SLOW-add/FAST-evict design. A fired assertion here is the intended human-review signal, not a bug. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; After: gPv2AllowListAuthentication_addSolver_asGuardian; Sources: ADV-08, GOV-09)
- [x] **SP-22** — While X-1 holds (`manager() == address(allowListGuardian)`), the guardian's instant `removeSolver` can never be blocked, delayed, or reverted by anything the timelock does or fails to do — the fail-safe eviction liveness. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — AllowListGuardian.sol:121-124 (removeSolver has no timelock reference, G-2), x-ray X-2; Priority: HIGH; After: allowListGuardian_removeSolver; Sources: ADV-09)
- [x] **SP-23** — [EXPECTED-VIOLATED LEAD] X-2's "a frozen timelock never blocks eviction" is only conditional: the timelock's own `AllowListGuardian.setManager(x != Guardian)` moves `manager` off the Guardian, after which every guardian `removeSolver` reverts — the fast-evict fail-safe is silently disabled by ordinary governance. A fired assertion is the intended lead. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; After: allowListGuardian_setManager then allowListGuardian_removeSolver; Sources: ADV-10)
- [x] **SP-24** — [EXPECTED-VIOLATED LEAD] A direct `authentication.setManager(0)` bricks BOTH the direct `onlyManager` path AND the Guardian's forwarding path (the Guardian is no longer manager) — a total, temporary system-wide DoS of add AND remove in a single call. A fired assertion is the intended lead (recoverable — see SP-25). (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; After: gPv2AllowListAuthentication_setManager_zero then Guardian handlers; Sources: ADV-11)
- [x] **SP-25** — No matter the current `manager` value (including 0 from SP-24), the EIP-1967 admin can always successfully `setManager` to recover — the admin branch of `onlyManagerOrOwner` never depends on `manager`, and no in-scope function rewrites the admin slot; the SP-24 brick is never permanent. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — GPv2AllowListAuthentication.sol:89-95; setAdmin never called in scope; Priority: HIGH; After: gPv2AllowListAuthentication_setManager_asAdmin; Sources: ADV-12)
- [x] **SP-26** — [EXPECTED-VIOLATED LEAD] Once X-1 is broken via an authorized handoff, exactly 3 of the Guardian's 4 functions (addSolver/removeSolver/setManager) degrade to permanently-reverting dead code (they forward and fail one level deeper at the authenticator's manager/admin gate), the Guardian cannot self-heal via its own setManager, `setGuardian` still works, and the new manager (even GUARDIAN_ROLE or an actor) gains raw instant add/remove — contradicting the NatSpec fail-safe claim. A fired assertion is the intended composability lead. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; After: setManager/acceptManagership handlers then Guardian-fn probes; Sources: GOV-09, ADV-08, ADV-10)
